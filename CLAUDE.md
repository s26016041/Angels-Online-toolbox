# 抓數值的鐵則（本專案最高原則）

目標：官方**小更新**（新增道具／地圖／技能／獎勵格、.data 位移、函式搬家）不准讓任何
功能壞掉或算錯；允許壞的只有**大更新改底層**（函式本體重寫、物件版面／欄位偏移改變）。
所有數值都要**準確**：不能猜、不能推理、不能用「看起來像」「編號應該連號」的值。

## 資料來源優先序（高 → 低，動手前先走完這個判斷）

1. **首選：執行時讀遊戲自己載進記憶體的資料**（怪物範本表、背包結構、兌換表、
   地形圖、精靈變數樹都是這樣做的）。改版自動跟上，零維護。
   **要寫死任何遊戲資料之前，先確認遊戲有沒有把那份資料載進記憶體**
   （`monsters.py`、`bag.py` 的品質／耐久、`exchange.py` 的編號反查是正面範本）。
2. **位址一律 AOB 自動定位**：任何 angel.dat 內的函式位址／全域／vtable 常數，
   必須登記進 `app/game/locate.py` 的 SIGS，由 `locate.warm()` 開機寫回。
   - 同一個位址**不准在第二個地方再寫一次**（位移後另一份不會跟上，也不會警告）。
   - 特徵不准把答案寫死在裡面（模組內 4-byte 立即值交給 `_auto_mask` 遮、rel32 一律遮）；
     靠指令骨架當錨。
   - 定位失敗要**大聲**：fn 類清 0（`Mover.call()` 擋下）、data 類留舊值靠讀取端驗證；
     `moved()` / `failed()` 必須看得到。
   - 加完／改完特徵要跑 `tools/verify_sigs.py`（唯一性＋真改版模擬）與 `tools/selfcheck.py`。
3. **確認過真的不在記憶體裡的資料，才准抄遊戲資源包（GAMEDATA/setting）**，而且：
   - 用 `tools/build_*.py` 從資源包**自動抽**，不准手打、不准從編號規律推。
   - 查不到／對不上 → **拒絕動作或安全退化**（顯示編號、當一般物品、功能停用），
     絕不拿猜的值送給遊戲（`daily_tab` 的兌換編號記憶體反查、對不上就拒送，是範本）。
   - 每一份寫死表都要登記到 memory 的 `items-table-maintenance`，改版後重跑 build 工具。
   - 過期偵測：`assets/table_stamp.json` 記「資料表是對哪一版遊戲核對的」
     （`app/game/tablestamp.py`）。遊戲換版時掛機分頁會亮警示，重跑 build 工具
     核對後執行 `py tools\stamp_tables.py` 蓋章才熄。

## 硬規則

- **ID 不照系列連號，不能猜**；不確定一律留空——留空＝安全退化，填錯＝很有自信的錯。
- **交給遊戲的位址／背包格號，送出前當場重讀重驗**（`energy.decompose` 是範本），
  不能信先前那一拍讀到的（背包會在中間變動、實體會被回收）。
- 讀取端一律做合理性驗證（指標範圍 `0x10000~0x7FFF0000`、vtable 對得上、數值範圍、
  兩份欄位交叉比對），驗不過就退安全預設，不准拿垃圾值繼續算。
- 結構偏移（`+0xBC` 這類）與函式呼叫版面**允許**寫死——那本來就屬於「大更新才會壞」
  的類別；但必須留反組譯出處註解，改版後靠 `game-patch-relocate` 的流程重驗。
- 失效模式只允許兩種：**大聲停用**（警示＋功能不動）或**安全退化**（顯示編號／少做事）。
  「安靜地做錯事」（送錯格、傳錯圖、算錯百分比）一律當 bug 修。

## 介面文字：不准把開發工作丟給使用者（2026-08-27 使用者指正）

使用者是**用**這個工具箱的人，不是維護它的人。所以介面上任何看得到的訊息
（狀態列、警示、通知、tooltip、對話框）：

- ⛔ **不准出現要他自己跑的指令或原始碼位置**——`py tools\xxx.py`、
  `tools/build_*.py`、`app/game/items.py`、`spec`／`collect_submodules`
  這類一律不准。那些是**我的工作**。
- ✔ 只講三件事：**發生什麼事、要不要緊、他要不要做什麼**。
  需要我出手時就寫「跟 Claude 說一聲做改版體檢」——他只要轉述一句話。
- ⛔ **訊息不准點名它自己沒驗過的東西。** 資料表過期警示只是比映像 CRC 的
  代理訊號，根本不知道哪一張表壞了，舊版卻寫死「技能射程／趴趴GO地圖等」
  ——而那兩張當下其實全對，真正對不上的是另一張。點名沒驗過的＝把人導向
  錯的地方。不知道就說不知道。
- ★ 對應的自動化：這類警示出現時**我自己去查**（`/_patchCheck` 的④層），
  查完直接告訴他結論，不要把工具名丟給他。

⚠ 加任何新的使用者可見文字之前，先照這一節自己檢查一遍。
（另見 memory 的 `ui-tooltip-short`：全部提示 1~3 行、關鍵資訊擺第一行。）
