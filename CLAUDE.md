# 抓數值的鐵則（本專案最高原則）

目標：官方**小更新**（新增道具／地圖／技能／獎勵格、.data 位移、函式搬家）不准讓任何
功能壞掉或算錯；允許壞的只有**大更新改底層**（函式本體重寫、物件版面／欄位偏移改變）。
所有數值都要**準確**：不能猜、不能推理、不能用「看起來像」「編號應該連號」的值。

## 資料來源優先序（高 → 低，動手前先走完這個判斷）

0. **★ 有表對表：GAMEDATA 解包的資料表是最權威的來源、不會錯**（使用者 2026-09-06 定：
   「有表對表，GAMEDATA 這是最權威不會錯，規則要改」；同 2026-08-25 的「表就是權威」）。
   - **靜態定義**（道具名稱／說明／效果、圖示、傳送點、技能、地圖名、商店表……）只要
     GAMEDATA 有，一律用 `tools/build_*.py` **自動抽成表**、程式查表；⛔ 不准拿記憶體讀到的
     值去推翻表、不准在表跟記憶體對不上時「改讀記憶體」或加「表過期？」的警示。
   - 表沒那筆（改版新增、還沒重跑 build）→ 安全退化（顯示編號／少做事），或退回讀記憶體
     當**備援**，不是反過來。
   - **每加一張表／一支 build 工具，當場做兩件事**：登記進 `.claude/commands/_patchCheck.md`
     第 7 步的資料表清單（寫清楚「改版後要重跑 `py tools\build_xxx.py` 重新生成」），以及
     memory 的 `items-table-maintenance`。⛔ 少登記＝改版後那張表沒人知道要重跑。
1. **執行時狀態讀記憶體**（背包裡有什麼、座標、HP、實體、快捷欄、精靈變數樹、
   地形圖這類「當下的狀態」），以及 GAMEDATA 裡**沒有**的定義（怪物範本表、兌換表）。
   改版自動跟上，零維護；但它是狀態的來源，不是拿來對表的裁判。
2. **位址一律 AOB 自動定位**：任何 angel.dat 內的函式位址／全域／vtable 常數，
   必須登記進 `app/game/locate.py` 的 SIGS，由 `locate.warm()` 開機寫回。
   - 同一個位址**不准在第二個地方再寫一次**（位移後另一份不會跟上，也不會警告）。
   - 特徵不准把答案寫死在裡面（模組內 4-byte 立即值交給 `_auto_mask` 遮、rel32 一律遮）；
     靠指令骨架當錨。
   - 定位失敗要**大聲**：fn 類清 0（`Mover.call()` 擋下）、data 類留舊值靠讀取端驗證；
     `moved()` / `failed()` 必須看得到。
   - 加完／改完特徵要跑 `tools/verify_sigs.py`（唯一性＋真改版模擬）與 `tools/selfcheck.py`。
3. **抄 GAMEDATA 的規矩**（第 0 條的做法細節）：
   - 用 `tools/build_*.py` 從資源包**自動抽**，不准手打、不准從編號規律推。
   - 查不到／對不上 → **拒絕動作或安全退化**（顯示編號、當一般物品、功能停用），
     絕不拿猜的值送給遊戲（`daily_tab` 的兌換編號記憶體反查、對不上就拒送，是範本）。
   - 每一份寫死表都要登記到 memory 的 `items-table-maintenance`，改版後重跑 build 工具。
   - 過期偵測：`assets/table_stamp.json` 記「資料表是對哪一版遊戲核對的」
     （`app/game/tablestamp.py`）。遊戲換版時掛機分頁會亮警示，重跑 build 工具
     核對後執行 `py tools\stamp_tables.py` 蓋章才熄。

## 硬規則

- **ID 不照系列連號，不能猜**；不確定一律留空——留空＝安全退化，填錯＝很有自信的錯。
- **交給遊戲的位址／背包格號，送出前當場重讀重驗**（`energy.decompose` 是範本），
  不能信先前那一拍讀到的（背包會在中間變動、實體會被回收）。
- 讀取端一律做合理性驗證（指標範圍 `0x10000~0x7FFF0000`、vtable 對得上、數值範圍、
  兩份欄位交叉比對），驗不過就退安全預設，不准拿垃圾值繼續算。
- 結構偏移（`+0xBC` 這類）與函式呼叫版面**允許**寫死——那本來就屬於「大更新才會壞」
  的類別；但必須留反組譯出處註解，改版後靠 `game-patch-relocate` 的流程重驗。
- 失效模式只允許兩種：**大聲停用**（警示＋功能不動）或**安全退化**（顯示編號／少做事）。
  「安靜地做錯事」（送錯格、傳錯圖、算錯百分比）一律當 bug 修。

## 版控：每改完一個段落**自己 commit + push**（2026-08-27 使用者明令）

> 「以後每改完一個段落你必須自己 push，這要寫進規則。」

- **一個段落** ＝ 一件能獨立講清楚的修正或功能，**而且已經驗過**
  （相關的 `tools\*_check.py`、`smoke_ui.py`、`py_compile` 有跑過）。
  做完就 `git commit` 接著 `git push origin main` —— **不必問、不必等我提醒**。
- ⛔ **不准累積一大包再一次推**。一次推一件事，訊息才講得清楚、出事才回得去。
- ⛔ **驗沒過不准推**。壞的東西留在本機修好再推，不要推上去再修。
- ⚠ **`git commit` 與 `git push` 是常設授權；建 GitHub Release 仍然要先問**
  （見 memory `ask-before-release`：發版會強制所有人自動更新，時機由使用者決定）。
- ⚠ 不屬於這次工作的既有改動（別人／先前 session 留在工作目錄的檔）**不要順手
  一起 commit** —— 先問一句。

## Memory：告一段落就**自己存**（2026-09-01 使用者明令）

> 「memory 你覺得告一段落就自己存，寫進專案規則。」

- **一個段落**＝一件事已經有可以交代的結論（做完、驗過、確定是死路、或規格談定），
  就**當場寫進 memory** —— 不必問、不必等使用者提醒、不必等整個 session 結束。
- 存的內容照全域規定：走不通的路與被否決的方案也要寫，避免下個 session 重踩。
- 存完在 `MEMORY.md` 補一行索引；已有同主題的檔就**改那一份**，不要開重複的。
- ⚠ 只是「查到一半、中間狀態」不算段落 —— 那種還不值得寫成 memory。
