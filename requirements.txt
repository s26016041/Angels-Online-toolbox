# GUI 框架（必要）
PySide6>=6.6

# 全背景視窗控制與擷取（不佔用滑鼠鍵盤的核心）
pywin32>=306
Pillow>=10.0

# 圖像比對：偵測登入畫面 / 斷線畫面（狀態偵測用）
opencv-python>=4.8
numpy>=1.24

# 記憶體讀取（未來經驗計算功能會用到，先列入）
pymem>=1.13.1

# 備援方案：若遊戲不吃 PostMessage，退回「切前景輸入」時才需要
# pyautogui>=0.9.54
